## 리팩토링 시 지켜야 할 규칙

1. Setter 대신 의도가 드러나는 비즈니스 메서드로 상태 변경 (예: `order.cancel()`)
2. 모든 연관관계는 LAZY(지연 로딩)로 설정하고, 필요한 조회는 fetch join으로 해결
3. 컬렉션 필드는 필드 레벨에서 바로 초기화
4. 연관관계의 주인은 외래 키가 있는 쪽으로 정하기 (비즈니스 중요도로 정하지 않기)
5. 양방향 매핑 시 양쪽에 값을 함께 설정하는 연관관계 편의 메서드 작성
6. `@ManyToMany` 사용 지양, 연결 엔티티로 일대다·다대일로 풀어내기
7. Repository 계층은 엔티티 매니저만 사용, 비즈니스 로직 포함 금지
8. Service 계층은 `@Transactional(readOnly=true)` 기본, 변경 메서드만 별도 `@Transactional`
9. Controller는 엔티티 직접 반환 금지, DTO로 변환해서 노출
10. 상속 구조가 필요한 도메인은 상속 매핑 전략(조인/단일테이블/구현클래스별) 명확히 선택
11. 값 타입(Address 등)은 Setter 제거하고 생성자로만 초기화해 불변 객체로 설계
12. 식별자는 자연키 대신 Long 대체키 + 키 생성 전략 사용
13. 동시성 문제 대비해 DB 레벨 유니크 제약조건 등 안전장치 추가